#pragma once

#include <vector>

namespace tmr {

	extern std::size_t SEQUENTIAL_STEPS;
	extern std::size_t INTERFERENCE_STEPS;
	extern std::size_t INTERFERENCE_SKIPPED;

	extern std::size_t SUMMARIES_NEEDED;
	extern std::size_t ABA_AWARENESS_CHECKS;
}